package scopes

func hello() {
    var i int
    bello := func() {
        var U, V, W float64
        cello := func() {
            var k = 0
        }
    }
    gello := func() {
        var x, y float64 = -1, -2
        yello := func() {
            var a, b int
            gello := "hi"
        }
    }
    var (
        i       int
        _, v, s = 2.0, 3.0, "bar"
    )
    gello := "hi"
    // var i float32
    // var re, im = complexSqrt(-1)
    //var _, found = // entries[name]  // map lookup; only interested in "found"
}
