package var_decs

import "fmt"

func hello() {
    var i int
    var U, V, W float64
    var k = 0
    var x, y float64 = -1, -2
    var (
        i       int
        _, v, s = 2.0, 3.0, "bar"
    )
    // var i float32
    // var re, im = complexSqrt(-1)
    //var _, found = // entries[name]  // map lookup; only interested in "found"
}
